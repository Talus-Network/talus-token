[package]
name = "faucet"
edition = "2024.beta" # edition = "legacy" to use legacy (pre-2024) Move
authors = ["Talus Labs <hi@taluslabs.com>"]
[dependencies]
Talus = {local = "../talus"}

[addresses]
faucet = "0x0"